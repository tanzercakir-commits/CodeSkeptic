# NASA fprime idiom profile — the scan is CLEAN (10 -> 0) with F prime's
# assert handler declared: SwAssert deliberately lacks [[noreturn]], so
# declaring it fatal kills the failure path and guarded code stops
# warning.
fatal_asserts = SwAssert
