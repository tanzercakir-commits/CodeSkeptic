# Expected corpus finding counts (validated by run_corpus.sh, 10%+2 tolerance).
# Versions are pinned, so deviation signals a semantic regression:
# a drop = silent finding loss, a rise = FP explosion. On deliberate rule
# changes this file is updated in the SAME PR (the rationale stays
# visible in the commit message).
#
# 2026-07-12 (report-flood dedup): warning-severity null-derefs report
# once per (variable, function); later sites become trace notes on the
# first finding. Same-variable floods collapse: cjson 123 -> 55,
# abseil 12 -> 5. tinyxml2 (9, distinct variables) and catch2 (0)
# unchanged. Case-level Juliet metrics preserved (verified locally:
# all five floors green).
#
# 2026-07-12 (disjuncts v2b + comparator-ladder gating): cjson
# 55 -> 50 (flag/status correlations and two cmp-ladder shapes died);
# abseil 5 -> 4. Pins re-centered so the tolerance band tracks the
# improved level instead of sagging toward its lower edge.
#
# 2026-07-15 (bounds rule v0): cjson 50 -> 53. The +3 are TRUE POSITIVES,
# all the same real bug: cJSON vendors the Unity test framework, whose
# tutorial file tests/unity/examples/example_{1,2,3}/src/ProductionCode.c
# ships FindFunction_WhichIsBroken with a deliberate off-by-one — missing
# braces run the `if` after `while (i <= 8)`, so NumbersToFind[9] reads
# one past a 9-element array. The bounds rule proves i in [9,+inf] on loop
# exit and flags the definite OOB. Pin raised to track the correct level.
#
# project finding-count
cjson 53
tinyxml2 9
# abseil runs only with CORPUS_DEEP=1 (weekly cron; tag 20260526.0).
# 4 = post-v2b level; 5 was post-dedup; 12 was the post-FP-hunt level
# (2026-07-12): builtin_expect transparency, static/global exemption,
# member/receiver escape.
abseil 4
# catch2 (v3.15.2, deep-only): zero findings — the FP-explosion tripwire.
catch2 0
