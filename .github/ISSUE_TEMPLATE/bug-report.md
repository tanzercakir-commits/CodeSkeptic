---
name: Bug report
about: Crashes, wrong exit codes, broken output, build problems
title: "[BUG] "
labels: bug
---

**What happened / what you expected:**

**Exact command** (and `.codeskeptic.conf` if present):

```
```

**Output** (console; for crashes include the last lines before the
crash; for report bugs attach the JSON/SARIF/HTML):

```
```

**Version** (`codeskeptic --version`), platform, and install method
(release tarball / Docker / source build — for source: LLVM version):

**Reproduction** (a minimal file or repo we can run against — the
analyzer's own broken-TU guard means we also need to know whether the
input compiles):
