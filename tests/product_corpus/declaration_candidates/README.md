# Recorded declaration candidates

These28 additive records select14 observed non-SQLite library APIs on each of
Linux x86-64 and macOS arm64. They bind the unchanged draft model and the actual
independently audited8953056 hosted v2 packets by digest. They are prospective
inputs to independent declaration review, not independent decisions themselves.

Use `scripts/product_profiles.py native-declaration-candidate-check` with the
explicit repository-relative `--declaration-candidate` path. The reader requires
the actual linked packet at its recorded absolute evidence path; absent evidence
fails closed, and the command does not download anything. Packet locations are
local evidence storage identities, not portable runtime configuration.

Each projection retains the complete selected request, every target and the
freshly recomputed issues. Assessments identify actual canonical declarations,
complete captured signatures, recorded defining headers and visibility limits.
Empty issues or a successful candidate check is not an independent acceptance.
An external exact-record/source-head review and its digest must be supplied
explicitly to obtain the narrow `declaration_evidence_reviewed` fact.

Do not overwrite these records or their historical packets after review.
No declaration-review index or aggregate model-freeze path is introduced here.
The historical50 library+3 entry obligation is unchanged; reviewed identities
are not qualified pairs. Windows, SQLite, entry review, additional transfer/API
selection, source/sink/sanitizer semantics and complete corpus/security-fix/safe
control acceptance remain separate obligations. No detector is implemented or
measured by these records; U003 is not complete and U004 has not begun.
