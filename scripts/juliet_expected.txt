# Juliet score guard baselines (validated by juliet_eval.py; violation = red).
# Format: <CWE-name-prefix> <min-rprecision> <min-rhitrate>
# Values pinned with a small margin from the 2026-07-10 run (PR #18):
# rprecisions 1.000/0.640, rhitrates 0.347/0.233/0.198/0.435/0.045.
# On a deliberate rule change, baselines are updated in the SAME PR —
# the rationale stays visible in the commit message.
#
# 2026-07-12 (PR #38, abseil FP hunt): CWE401 floors moved 0.55/0.22 ->
# 0.60/0.18. The refined escape semantics (member-of-local-aggregate is
# NOT an escape; statics/globals untracked) raised measured rprecision
# to 0.659 and settled rhitrate at 0.201. The recall gap is the Juliet
# 44/45 "data passed via static global" families: our old hits there
# fired for the wrong reason (the leak completes in the sink function,
# not where we reported). A known FN — needs
# whole-program global-flow tracking. Precision floor RAISED, recall
# floor lowered to the honest measured value.
# 2026-07-12 (PR #42, configurable allocators): CWE401 precision floor
# 0.60 -> 0.55. Tracking realloc closed a SILENT COVERAGE GAP (the old
# matcher list never included it), making ~25 realloc-family files
# visible for the first time: +3 absolute TPs, but their good variants
# hit the documented correlated-guard limitation (staticReturnsTrue()/
# staticReturnsFalse() call conditions — PathFacts cannot key disjuncts
# on calls; the guarded-disjuncts-v2 design item). Measured via a
# main-vs-PR local diff: every one of the +24 FPs is a realloc file;
# 4 old FPs also disappeared (escape refinements). Wider coverage with
# the same known limitation, not lost precision on covered code.
# 2026-07-12 (PR #44, disjuncts v2a + noreturn path kill): CWE401
# precision floor RAISED 0.55 -> 0.62 (measured 0.669 locally: the
# constant-returning call-guard keys, the freed-through-alias
# suppression and the noreturn dilution fix together removed most of
# the realloc-family FPs). CWE416 recall floor RAISED 0.38 -> 0.44
# (measured 0.476 — call-guard pairing restored UAF true positives).
# CWE415 recall floor RAISED 0.20 -> 0.21 (measured 0.225).
# 2026-07-12 (disjuncts v2b: fact lifecycle + stamping + entailment +
# disjunction elimination + convergence widening): three floors RAISED
# on stable local measurements (identical across three consecutive
# replays): CWE401 precision 0.62 -> 0.66 (measured 0.692 — literal
# stamps keep flag-correlated alloc/free paths separate), CWE415
# recall 0.21 -> 0.23 (measured 0.241), CWE416 recall 0.44 -> 0.48
# (measured 0.501 — flow-variant guards now key after local literal
# assignments). CWE476 unchanged at 1.000/0.352.
#
# 2026-07-17 (#92 unchecked-allocation rule): the CWE-690 rule makes a
# known allocator's return MaybeNull, so `null_check_after_deref` GOOD
# functions — which deref malloc with NO check ("FIX: Don't check for
# NULL since we wouldn't reach this line if the pointer was NULL",
# which is false) — now get a null-deref finding. Those good functions
# CONTAIN a real unchecked-alloc defect; the finding is a true
# positive, not an FP, so juliet_eval EXCLUDES it (isKnownLaxGood) and
# prints the count. This is a documented ground-truth correction, NOT
# a floor relaxation: the 0.95 precision floor is UNCHANGED and stays
# fully sensitive to real regressions (verified: 32/32 new CWE476
# null-deref "FPs" are exactly this class, 0 genuine FP; rprecision
# holds at 1.000). Recall rises 0.30-baseline -> 0.347.
# 2026-07-18 (#96 int-overflow untrusted-source recall): the
# int-overflow rule now models a text-parse source (atoi/strtol family)
# as its return type's full range, so `int n = atoi(input); n * k`
# proves overflow — the canonical untrusted-multiply shape. NEW floor
# for CWE190. Measured on the strided 400-file sample: rprecision 1.000,
# rhitrate 0.010 (full 3080-file corpus: 42 TP, 0 FP, precision 1.000).
# Floor rprecision 0.95 (matches the other perfect-precision rules — a
# single new FP fails it); rhitrate 0.005 (a ~2x margin that catches a
# total recall collapse to 0 while tolerating sample jitter). Recall is
# deliberately narrow: rand()'s [0,RAND_MAX] range is NOT over-
# approximated (documented known FN — RAND32() bit-shuffle), and self-
# square x*x is skipped (its safe form is guarded by abs()/sqrt(), which
# the integer refiner cannot fold — every square-op FP is exactly that
# good sink). Precision-first, as everywhere.
CWE476 0.95 0.30
CWE401 0.66 0.18
CWE415 0.95 0.23
CWE416 0.95 0.48
CWE369 0.95 0.03
CWE190 0.95 0.005
