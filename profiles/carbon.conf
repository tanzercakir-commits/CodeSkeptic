# carbon-lang idiom profile — CARBON_CHECK's failure path ends in
# CheckFailImpl, which never returns; declaring it fatal removes the
# post-check impossible paths (218-TU scan configuration).
fatal_asserts = CheckFailImpl
