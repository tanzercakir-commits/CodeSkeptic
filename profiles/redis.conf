# Redis idiom profile — the zmalloc allocation family. Honest status:
# idiom round 2 (assert vocabulary, sds strings) is still on the
# backlog; this profile covers the allocator layer that round 1 used.
alloc_functions = zmalloc,zcalloc,zrealloc,ztrymalloc,zstrdup
free_functions = zfree
